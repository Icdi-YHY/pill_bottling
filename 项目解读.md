# pill_bottling.v 项目逐行解读

> 基于 BUPT TEC-8 实验箱的 Verilog 药瓶灌装控制器  
> 四人分工：逐行标注每一行代码的责任人

---

## 1. 项目概述

**功能**：控制药瓶灌装流水线，从传送带上按设定数量（每瓶片数）装瓶，达到目标瓶数后自动停机。

**输入**：6 个按键（启动/暂停/设置/加减/查看）+ 1Hz 时钟 + 复位  
**输出**：4 位数码管（药片数 + 瓶数）+ 状态指示数码管 + LED 告警灯

**系统状态机**：`IDLE → WORK → PAUSE ↔ SET → DONE`

---

## 2. 四人分工总览

| 角色 | 负责模块 | 代码段 |
|------|---------|--------|
| **A — 顶层架构师** | 模块端口、寄存器声明、参数定义、状态常量 | L1–L48 |
| **B — 按键边缘检测** | 6 个按键的同步寄存器 + 下降沿检测 | L50–L78 |
| **C — 主状态机与灌装逻辑** | 5 状态跳转、参数调节、自动灌装计数流水线 | L80–L141 |
| **D — 显示译码与辅助函数** | 显示 MUX、状态译码、告警 LED、`split_bcd`、`seg7` | L143–L265 |

---

## 3. 逐行详解

### 【A 负责】L1–L48：顶层模块、端口与寄存器声明

```
module pill_bottling (
    input clk_1hz,
```
> **A 写** — 模块定义开始。`pill_bottling`，药瓶灌装控制器。时钟输入为 **1Hz**（区别于 digital_clock 的高频时钟方案），即每时钟上升沿代表 1 秒。

```
    input rst,
```
> **A 写** — 复位信号，**低电平有效**（注意后续 always 块的 `negedge rst` 说明了这一点）。这是 TEC-8 实验箱的硬件特性。

```
    input sta_k0,   // 启动
    input pau_k1,   // 暂停
    input set_k2,   // 设置 / 切换设置项
    input inc_k3,   // -1
    input dec_k4,   // +1
    input vie_k5,   // 查看设定值
```
> **A 写** — 6 个按键输入，分别对应 TEC-8 实验箱的 K0–K5：
> - `sta_k0` — 启动灌装
> - `pau_k1` — 暂停灌装
> - `set_k2` — 进入/切换设置模式
> - `inc_k3` — 数值 +1
> - `dec_k4` — 数值 -1
> - `vie_k5` — 查看设定值（按住时显示目标值而非实时值）

```
    output reg [3:0] pill_ten,
```
> **A 写** — 药片数个位（数码管显示）。

```
    output reg [3:0] pill_one,
```
> **A 写** — 药片数十位（数码管显示）。

```
    output reg [3:0] bottle_ten,
```
> **A 写** — 药瓶数个位（数码管显示）。

```
    output reg [3:0] bottle_one,
```
> **A 写** — 药瓶数十位（数码管显示）。

```
    output reg [3:0] state,
```
> **A 写** — 系统状态输出（单独一个数码管，显示状态编号：0=空闲, 1=运行, 2=暂停, 3/5=设置, 4=完成）。

```
    output reg [6:0] alarm
);
```
> **A 写** — 7 位告警/指示 LED 输出，用于直观查看当前处于哪种工作模式。

```
// 当前值寄存器
reg [6:0] cur_pills;
reg [6:0] cur_bottles;
```
> **A 写** — 当前实时计数寄存器：
> - `cur_pills`（7bit，范围 0–99）：当前已灌入此瓶的药片数
> - `cur_bottles`（7bit，范围 0–99）：当前已完成的药瓶数

```
// 设定值寄存器
reg [6:0] set_pills;
reg [6:0] set_bottles;
```
> **A 写** — 用户设定的目标值寄存器：
> - `set_pills`（7bit）：每瓶目标片数（默认 10）
> - `set_bottles`（7bit）：目标瓶数（默认 5）

```
// ====================================================
// 系统状态机定义
// ====================================================
reg [2:0] sys_state;
```
> **A 写** — 3bit 状态寄存器，保存系统当前所处状态。

```
localparam IDLE  = 3'd0;
localparam WORK  = 3'd1;
localparam PAUSE = 3'd2;
localparam SET   = 3'd3;
localparam DONE  = 3'd4;
```
> **A 写** — 5 个状态常量定义：
> - `IDLE`（0）：空闲，等待启动
> - `WORK`（1）：正在灌装
> - `PAUSE`（2）：暂停
> - `SET`（3）：设置参数
> - `DONE`（4）：全部完成

```
// 设置子状态：0-设置每瓶片数，1-设置目标瓶数
reg set_mode; 
```
> **A 写** — 设置子状态标记：`0` 表示正在调"每瓶片数"，`1` 表示正在调"目标瓶数"。默认先进入调片数模式。

---

### 【B 负责】L50–L78：按键边缘检测

```
// ====================================================
// 按键边缘检测 (针对 1Hz 慢时钟的同步边缘触发寄存器)
// ====================================================
reg r_sta_k0, r_pau_k1, r_set_k2, r_inc_k3, r_dec_k4;
```
> **B 写** — 声明 5 个**同步缓存寄存器**，每个按键对应一个，用于缓存按键前一拍的状态以检测边沿。注意 `vie_k5`（查看键）不需要边缘检测——它直接读电平值实现"按住查看"效果。

```
always @(posedge clk_1hz or negedge rst) begin
```
> **B 写** — 同步寄存器 always 块。时钟上升沿触发，**复位低电平有效**（`negedge rst`），与 TEC-8 实验箱硬件一致。

```
    if(!rst) begin
        r_sta_k0 <= 1'b1; 
        r_pau_k1 <= 1'b1; 
        r_set_k2 <= 1'b1;
        r_inc_k3 <= 1'b1; 
        r_dec_k4 <= 1'b1;
```
> **B 写** — 复位时所有缓存寄存器置为 `1'b1`。因为按键接到 GND（按下 = 0，松开 = 1），初始化为 1 表示"按键已松开"的稳态。

```
    end else begin
        r_sta_k0 <= sta_k0; 
        r_pau_k1 <= pau_k1; 
        r_set_k2 <= set_k2;
        r_inc_k3 <= inc_k3; 
        r_dec_k4 <= dec_k4;
```
> **B 写** — 每拍将按键当前电平存入缓存寄存器。下一拍时 `r_*` 保存的是上一拍的值，与当前值比较就能检测边沿。

```
    end
end
```
> **B 写** — always 块结束。

```
// 下降沿有效 (捕捉按键按下瞬间)
wire sta_edge = (r_sta_k0 && !sta_k0);
wire pau_edge = (r_pau_k1 && !pau_k1);
wire set_edge = (r_set_k2 && !set_k2);
wire inc_edge = (r_inc_k3 && !inc_k3);
wire dec_edge = (r_dec_k4 && !dec_k4);
```
> **B 写** — 5 条**下降沿检测**组合逻辑赋值：
> - `r_*` = 上一拍的值（寄存器输出），`!*_k*` = 当前值取反
> - 当上一拍为 1（松开）、当前为 0（按下）→ `1 && 1 = 1`，检测到下降沿
> - 组合逻辑，无时钟延迟，一个节拍内就能触发后续逻辑

---

### 【C 负责】L80–L141：主状态机与灌装逻辑

```
// ====================================================
// 主状态机与核心计数逻辑
// ====================================================
always @(posedge clk_1hz or negedge rst) begin
```
> **C 写** — 主时序逻辑 always 块。1Hz 时钟上升沿触发，低电平复位。

```
    if(!rst) begin
        cur_pills   <= 7'd0;
        cur_bottles <= 7'd0;
        set_pills   <= 7'd10; // 默认每瓶10片
        set_bottles <= 7'd5;  // 默认目标5瓶
        sys_state   <= IDLE;
        set_mode    <= 1'b0;
```
> **C 写** — 复位初始化：
> - 当前药片数、瓶数清 0
> - 默认每瓶 10 片，目标 5 瓶
> - 状态回到 `IDLE`
> - 设置子模式为调片数（0）

```
    end else begin
        
        // 1. 状态跳转与参数调整分支
        case(sys_state)
```
> **C 写** — case 语句根据当前状态分发处理逻辑。

```
            IDLE: begin
                cur_pills   <= 7'd0;
                cur_bottles <= 7'd0;
```
> **C 写** — 空闲状态下，持续将计数值清零（防止上次残留，同时保证每次进入 WORK 时都是从 0 开始）。

```
                if(sta_edge)       sys_state <= WORK;
```
> **C 写** — 按下启动键 → 进入 WORK 工作状态，开始灌装。

```
                else if(set_edge) begin
                    sys_state <= SET;
                    set_mode  <= 1'b0; // 默认先进入"设置单瓶片数"
                end
```
> **C 写** — 按下设置键 → 进入 SET 设置状态。`set_mode` 清 0，表示先进入"设置每瓶片数"。

```
            end
            
            WORK: begin
                if(pau_edge) begin
                    sys_state <= PAUSE;
```
> **C 写** — 工作状态下按下暂停键 → 进入 `PAUSE` 暂停状态，current 计数保持不变（下次恢复时继续）。

```
                end else if(cur_bottles >= set_bottles) begin
                    sys_state <= DONE;
```
> **C 写** — 当已完成的瓶数 ≥ 目标瓶数 → 进入 `DONE` 完成状态。注意这里使用 `>=` 而非 `==`，防止异常跳值导致卡死。

```
                end
            end
            
            PAUSE: begin
                if(sta_edge)      sys_state <= WORK;
```
> **C 写** — 暂停状态下按启动键 → 回到 `WORK` 继续灌装（计数值保留）。

```
                else if(set_edge) begin
                    sys_state <= SET;
                    set_mode  <= 1'b0;
                end
```
> **C 写** — 暂停状态下按设置键 → 进入 `SET` 调整参数，`set_mode` 清 0 从调片数开始。

```
            end
            
            SET: begin
                // 按下 SET 键顺序切换
                if(set_edge) begin
                    if(set_mode == 1'b0) begin
                        set_mode <= 1'b1; // 切换到"设置总瓶数"
```
> **C 写** — 设置状态下按 SET 键：若当前正调片数（`set_mode == 0`），切换到调瓶数（`set_mode <= 1`）。

```
                    end else begin
                        sys_state <= IDLE; // 设置完毕，回到空闲状态
                    end
                end
```
> **C 写** — 若当前已调瓶数（`set_mode == 1`），再按 SET 表示设置完成，回到 `IDLE` 空闲态，等待启动。

```
                
                // 参数加减调节控制
                if(set_mode == 1'b0) begin // 调整单瓶药片数目标限制 (1 ~ 99)
                    if(inc_edge && set_pills < 7'd99) set_pills <= set_pills + 7'd1;
                    if(dec_edge && set_pills > 7'd1)  set_pills <= set_pills - 7'd1;
```
> **C 写** — 调片数模式下：
> - `inc_edge`（按 +1 键）且未达上限 99 → 片数 +1
> - `dec_edge`（按 -1 键）且未达下限 1 → 片数 -1
> - 上下限保护：1 ≤ set_pills ≤ 99

```
                end else begin             // 调整目标总瓶数限制 (1 ~ 99)
                    if(inc_edge && set_bottles < 7'd99) set_bottles <= set_bottles + 7'd1;
                    if(dec_edge && set_bottles > 7'd1)  set_bottles <= set_bottles - 7'd1;
```
> **C 写** — 调瓶数模式下，逻辑同上，只是目标从 `set_pills` 换成 `set_bottles`。

```
                end
            end
            
            DONE: begin
                // 完成状态，不作跳出处理。直到硬件外部产生 rst 信号恢复到 IDLE
            end
```
> **C 写** — 完成状态是一个**终止态**：什么都不做，等待外部硬件复位（`rst` 按键）才能回到 IDLE。这避免了误触按键导致重新开始。

```
            
            default: sys_state <= IDLE;
        endcase
```
> **C 写** — 安全兜底：如果 `sys_state` 出现非法值（如 3'd5、3'd6、3'd7），回到空闲态。

```
        
        // 2. 自动药片流水线计数逻辑 (仅工作状态且瓶数未满)
        if(sys_state == WORK && cur_bottles < set_bottles) begin
```
> **C 写** — **自动灌装流水线核心**：只有在 WORK 状态且未达到目标瓶数时才计数。每 1Hz 时钟 +1 片。

```
            if(cur_pills + 7'd1 >= set_pills) begin
                cur_pills   <= 7'd0;
                cur_bottles <= cur_bottles + 7'd1;
```
> **C 写** — 如果当前瓶的药片数 +1 后 ≥ 目标片数 → 当前瓶装满了：
> - `cur_pills` 归 0（新瓶开始）
> - `cur_bottles` +1（已完成的瓶数 +1）
> 同样使用 `>=` 防止异常跳值导致无限装瓶。

```
            end else begin
                cur_pills   <= cur_pills + 7'd1;
```
> **C 写** — 如果还没装满 → 药片数 +1（继续向当前瓶装片）。

```
            end
        end
        
    end
end
```
> **C 写** — 主 always 块结束。

---

### 【D 负责】L143–L265：显示译码与辅助函数

```
// ====================================================
// 显示输出控制逻辑 (多路组合复用器)
// ====================================================

// 修复语法错误：result 寄存器必须定义在 always 块外部
reg [7:0] result_pills;
reg [7:0] result_bottles;
```
> **D 写** — 声明两个 8bit 中间寄存器。注意注释说明此处的声明位置是**故意放在 always 块外**的（Verilog 语法限制：`always @(*)` 中赋值的变量必须在外部声明为 reg）。

```
always @(*) begin
```
> **D 写** — **组合逻辑** always 块（`@(*)` 敏感所有输入信号），实现显示数据多路选择器。

```
    // 默认防锁存器清除赋值
    result_pills   = 8'd0;
    result_bottles = 8'd0;
```
> **D 写** — **锁存器避免**的标准写法：在 case/if 之前给所有赋值变量一个默认值，防止综合工具推断出锁存器（latch）。

```
    if(sys_state == SET) begin
        // 设置状态下：数码管直接实时显示正在修改的配置目标值
        result_pills   = split_bcd(set_pills);
        result_bottles = split_bcd(set_bottles);
```
> **D 写** — 设置模式下，数码管固定显示**目标设定值**（set_pills / set_bottles），方便用户边调边看。

```
    end else if(vie_k5 == 1'b0) begin
        // 任何非设置状态下按下【查看键】：切为显示设定目标值
        result_pills   = split_bcd(set_pills);
        result_bottles = split_bcd(set_bottles);
```
> **D 写** — `vie_k5` 是**低电平有效**（按下 = 0）。在任何非设置状态下按住查看键，数码管显示设定目标值，松开后切回实时值。实现"临时查看"功能。

```
    end else begin
        // 默认运行状态下：持续输出当前的实时运行计数值
        result_pills   = split_bcd(cur_pills);
        result_bottles = split_bcd(cur_bottles);
```
> **D 写** — 默认情况下（非设置 + 未按查看键），显示当前实时灌装计数。

```
    end
    
    // 总线分拆到十位与个位输出信号
    pill_ten    = result_pills[7:4];
    pill_one    = result_pills[3:0];
    bottle_ten  = result_bottles[7:4];
    bottle_one  = result_bottles[3:0];
```
> **D 写** — 将 8bit 的 BCD 合并数拆成高位（十位）和低位（个位），分别赋给 4 个数码管输出口。`[7:4]` = 十位，`[3:0]` = 个位。

```
end
```
> **D 写** — 组合逻辑 always 块结束。

```
// ====================================================
// 7段共阴极数码管译码与警报状态指示
// ====================================================

// 单独状态数码管显示处理
always @(*) begin
    case(sys_state)
        IDLE:    state = 4'd0; // 空闲状态：显示字形 0
        WORK:    state = 4'd1; // 运行状态：显示字形 1
        PAUSE:   state = 4'd2; // 暂停状态：显示字形 2
        SET:     state = (set_mode == 1'b0) ? 4'd3 : 4'd5; // 调片数显3，调瓶数显5
        DONE:    state = 4'd4; // 完成状态：显示字形 4
        default: state = 4'd0;
    endcase
end
```
> **D 写** — 状态指示数码管译码器：
> - 5 个状态分别映射到 0–4 的数码管字形
> - SET 状态特殊处理：`set_mode==0`（调片数）→ 显示"3"，`set_mode==1`（调瓶数）→ 显示"5"
> - 数字 3 和 5 在数码管上形状差异明显，帮助用户区分当前在调哪个参数

```
// 模式/告警输出总线控制 (用于指示当前是否属于查看模式/设置模式)
always @(*) begin
    if(sys_state == SET) begin
        alarm = 7'b1010101; // 设置模式：LED交替发光
```
> **D 写** — 设置模式下，7 个 LED 交替亮灭（1010101），视觉上形成"闪烁条纹"效果，一眼就能看出正在设置。

```
    end else if(vie_k5 == 1'b0) begin
        alarm = 7'b1111111; // 运行中看目标值：LED全亮提示
```
> **D 写** — 按住查看键时，7 个 LED 全亮，提示当前数码管显示的是目标值而非实时值。

```
    end else if(sys_state == DONE) begin
        alarm = 7'b1111111; // 满载完成：LED全亮提示
```
> **D 写** — 灌装完成时 LED 全亮，作为"完工提醒"。

```
    end else begin
        alarm = 7'b0000000; // 正常工作运行实时值：LED全灭
    end
end
```
> **D 写** — 默认正常运行时，LED 全灭，表示一切正常、显示的是实时值。

```
// ====================================================
// 工具辅助函数 (Functions)
// ====================================================

// 0-99 纯组合逻辑高速减法实现 BCD 拆分（规避硬件除法器，100%可综合）
function [7:0] split_bcd;
    input [6:0] num;
    reg [3:0] ten;
    reg [3:0] one;
    begin
```
> **D 写** — 定义 `split_bcd` 函数：输入 7bit 数值（0–99），输出 8bit BCD 码（高 4bit 十位，低 4bit 个位）。
> - 纯组合逻辑实现，没有除法器（FPGA 上除法器资源昂贵且综合慢）
> - 用一系列 `if-else` 减法判断替代除法运算，100% 可综合

```
        if(num >= 90) begin
            ten = 9;  one = num - 90;
        end else if(num >= 80) begin
            ten = 8;  one = num - 80;
        end else if(num >= 70) begin
            ten = 7;  one = num - 70;
        end else if(num >= 60) begin
            ten = 6;  one = num - 60;
        end else if(num >= 50) begin
            ten = 5;  one = num - 50;
        end else if(num >= 40) begin
            ten = 4;  one = num - 40;
        end else if(num >= 30) begin
            ten = 3;  one = num - 30;
        end else if(num >= 20) begin
            ten = 2;  one = num - 20;
        end else if(num >= 10) begin
            ten = 1;  one = num - 10;
        end else begin
            ten = 0;  one = num;
        end
```
> **D 写** — 从 90 向下逐档减法：
> - 先判断是否 ≥ 90 → 十位=9，个位=num-90
> - 再判断 ≥ 80 → 十位=8，个位=num-80
> - ... 直到 ≥ 10 → 十位=1，个位=num-10
> - 最后 < 10 → 十位=0，个位=num
> - **特点**：从大到小判断，每档只需要一次减法，延迟路径最短，综合速度快。

```
        split_bcd = {ten, one};
    end
endfunction
```
> **D 写** — 拼接十位和个位为 8bit 输出：`{ten, one}` = `{4bit十位, 4bit个位}`。函数结束。

```
// 7段数码管经典真值表 (共阴极格式，1点亮，0熄灭)
function [6:0] seg7;
    input [3:0] num;
    begin
        case(num)
            4'd0: seg7 = 7'b0111111;
            4'd1: seg7 = 7'b0000110;
            4'd2: seg7 = 7'b1011011;
            4'd3: seg7 = 7'b1001111;
            4'd4: seg7 = 7'b1100110;
            4'd5: seg7 = 7'b1101101;
            4'd6: seg7 = 7'b1111101;
            4'd7: seg7 = 7'b0000111;
            4'd8: seg7 = 7'b1111111;
            4'd9: seg7 = 7'b1101111;
            default: seg7 = 7'b0000000;
        endcase
    end
endfunction
```
> **D 写** — 7 段数码管**共阴极**译码真值表：
> - 共阴极：`1` = 点亮对应段，`0` = 熄灭
> - 段排列（假设标准 a-b-c-d-e-f-g 顺序）：`7'b0gfedcba`
> - 例如数字 0 → `7'b0111111` = a,b,c,d,e,f 亮，g 灭 → 显示 "0"
> - 数字 8 → `7'b1111111` = 全部 7 段亮
> - `default` 全灭，处理非法输入

```
endmodule
```
> **A 写** — 模块定义结束。整个 pill_bottling 模块完整闭合。

---

## 4. 四人协作关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                   pill_bottling.v 模块                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────┐    ┌──────────────┐    ┌──────────────────┐      │
│  │ A: 顶层    │    │ B: 按键检测   │    │ C: 主状态机      │      │
│  │ 端口声明   │    │ r_sta/r_pau  │    │ 5状态跳转        │      │
│  │ 寄存器定义  │◄──►│ r_set/r_inc  │◄──►│ 参数调节         │      │
│  │ 参数常量   │    │ r_dec        │    │ 自动计数流水线    │      │
│  └───────────┘    └──────┬───────┘    └────────┬─────────┘      │
│                          │                     │                 │
│                     sta_edge                 cur_pills          │
│                     pau_edge                 cur_bottles        │
│                     set_edge                 sys_state          │
│                     inc_edge                 set_mode           │
│                     dec_edge                                    │
│                          │                     │                 │
│                          ▼                     ▼                 │
│                   ┌──────────────────────────────────┐          │
│                   │ D: 显示译码 + 辅助函数             │          │
│                   │ 显示 MUX ─→ pill_ten/pill_one     │          │
│                   │          ─→ bottle_ten/bottle_one │          │
│                   │ 状态译码 ─→ state                 │          │
│                   │ 告警控制 ─→ alarm                 │          │
│                   │ split_bcd / seg7 函数             │          │
│                   └──────────────────────────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**数据流**：
1. **A** 定义好模块接口和所有"容器"（寄存器）
2. **B** 将 5 个物理按键信号转为干净的边沿脉冲（`*_edge`）
3. **C** 消费这些脉冲，驱动状态机跳转和灌装计数，更新 `cur_*`、`set_*`、`sys_state`
4. **D** 读取 `C` 更新的寄存器和 `B` 的 `vie_k5` 电平，经 MUX 选择和 BCD 拆分后驱动数码管和 LED

---

## 5. 设计亮点总结

| 设计决策 | 为何这样写 |
|---------|-----------|
| 按键**下降沿**检测 | TEC-8 按键按下 = GND（低电平），松开 = VCC（高电平） |
| 计数用 `>=` 而非 `==` | 防止异常跳值导致卡死，增加鲁棒性 |
| `split_bcd` 用减法链 | 纯组合逻辑，避免 FPGA 上昂贵的除法器 IP |
| `DONE` 不自动退出 | 防止误触，需人工复位确认 |
| `vie_k5` 直接读电平 | "按住查看"功能需要连续电平而非单次边沿 |
| 锁存器预防赋值 | `result_*` 在 always 块头赋默认值 0，避免综合出 latch |
| `SET` 状态双数字显示（3/5） | 让用户一目了然当前在调片数还是瓶数 |
